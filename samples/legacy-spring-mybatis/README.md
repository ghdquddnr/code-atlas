# Legacy Spring MyBatis Sample

This sample project is intentionally small. CodeAtlas uses it as the first deterministic analysis target.

It contains:

- Spring-style controller annotations
- Service-to-mapper calls
- MyBatis mapper interface
- MyBatis XML statements
- SQL touching multiple tables

Expected tables:

- `TB_ORDER`
- `TB_ORDER_ITEM`
- `TB_PRODUCT`
