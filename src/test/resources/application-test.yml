spring:
  datasource:
    url: jdbc:h2:mem:code_atlas_test;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop

code-atlas:
  llm:
    enabled: false
  github:
    enabled: false
